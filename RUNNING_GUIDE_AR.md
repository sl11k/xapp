# دليل تشغيل المشروع خطوة بخطوة

هذا الدليل يشرح **بالتفصيل** كيفية تشغيل المشروع على:

- الويب (المتصفح)
- الجوال عبر Expo Go (iPhone / Android)

***

## 1) المتطلبات الأساسية

قبل التشغيل تأكد من وجود:

1. **Node.js** (الإصدار 18 أو أعلى)
2. **npm** (يأتي مع Node.js)
3. اتصال إنترنت جيد (مهم جداً عند استخدام `--tunnel`)
4. تطبيق **Expo Go** على الجوال

للتحقق من التثبيت:

```bash
node -v
npm -v
```

***

## 2) فتح المشروع في المسار الصحيح

انتقل إلى مجلد المشروع الرئيسي:

```bash
cd c:\Users\m7md4\Downloads\rork-muwassa-business-hub-main\rork-muwassa-business-hub-main
```

ثم ادخل إلى مجلد تطبيق Expo:

```bash
cd expo
```

***

## 3) تثبيت الحزم (Dependencies)

نفّذ هذا الأمر مرة واحدة (أو عند تحديث المشروع):

```bash
npm install
```

إذا واجهت تعارضات تبعيات:

```bash
npm install --legacy-peer-deps
```

***

## 4) تشغيل المشروع (الطريقة الموصى بها)

### تشغيل Expo مع Tunnel (يدعم الجوال + الويب)

```bash
npx expo start --clear --tunnel
```

ماذا يفعل هذا الأمر؟

- `--clear`: يمسح كاش الباندلر ويحل مشاكل التحديثات القديمة
- `--tunnel`: يسهّل تشغيل المشروع على الجوال حتى لو الشبكة مختلفة

بعد التنفيذ ستظهر:

- صفحة ويب على رابط غالباً مثل: `http://localhost:8081`
- QR Code لتطبيق Expo Go

***

## 5) تشغيل المشروع على الويب

بعد تشغيل الأمر أعلاه:

1. افتح المتصفح
2. ادخل إلى الرابط:

```text
http://localhost:8081
```

إذا لم يفتح:

- انتظر اكتمال `Bundled` في الطرفية
- جرّب تحديث الصفحة
- تأكد أن المنفذ 8081 غير مشغول من برنامج آخر

***

## 6) تشغيل المشروع على iPhone / Android عبر Expo Go

1. افتح تطبيق **Expo Go** على الجوال
2. امسح QR Code الظاهر في الطرفية
3. انتظر التحميل الأول (قد يكون أبطأ قليلاً أول مرة)

ملاحظة مهمة:

- إذا لم يعمل QR مباشرة، تأكد من ثبات الإنترنت، ثم أعد التشغيل بالأمر نفسه مع `--tunnel`.

***

## 7) أوامر مفيدة أثناء التطوير

أثناء عمل `expo start` تستطيع استخدام:

- `r` لإعادة التحميل
- `w` لفتح الويب
- `a` لمحاكي Android
- `j` لفتح الـ debugger

***

## 8) حل المشاكل الشائعة بسرعة

### المشكلة: المنفذ 8081 مشغول

```bash
npx kill-port 8081
```

ثم أعد التشغيل:

```bash
npx expo start --clear --tunnel
```

### المشكلة: شاشة سوداء أو تغييرات لا تظهر

1. أوقف السيرفر (`Ctrl + C`)
2. أعد التشغيل مع تنظيف الكاش:

```bash
npx expo start --clear --tunnel
```

### المشكلة: الجوال لا يتصل

- تأكد من `--tunnel`
- أعد مسح QR
- تأكد من تحديث Expo Go لأحدث نسخة

***

## 9) ملاحظة عن Pull-to-Refresh Animation

ميزة السحب للتحديث مفعلة، وتعرض:

- أنيميشن Lottie
- نص حسب اللغة (`جار التحميل...` / `Reloading...`)
- اهتزاز خفيف على الأجهزة المدعومة

إذا لم تظهر الحركة مباشرة، نفّذ إعادة تشغيل نظيفة:

```bash
npx expo start --clear --tunnel
```

***

## 10) تسلسل التشغيل السريع (مختصر)

```bash
cd c:\Users\m7md4\Downloads\rork-muwassa-business-hub-main\rork-muwassa-business-hub-main\expo
npm install
npx expo start --clear --tunnel
```

ثم:

- الويب: `http://localhost:8081`
- الجوال: امسح QR من Expo Go
- <br />

